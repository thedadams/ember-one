# Images

Store shared screenshots, swatches, and project artwork here. App-specific
screenshots may instead live beside that app's theme files when they are only
useful for that port.

Prefer descriptive lowercase filenames, such as `vscode-dark-typescript.png`.
